# template-image-captioner

# Install

```
conda create -n image-captioner python==3.10
conda activate image-captioner
pip3 install -r requirements.txt
```


# Run on RUNPOD
```
bash -c "bash install.sh && gradio start_ui.py"
```

