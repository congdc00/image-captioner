models: 
  - "name_model"
config_model: "--sample_config xxx"
template_prompt: "Describe the setting, whether indoors or outdoors, the predominant colors of the image, and details about the shapes, colors, and positions of each object in the room, separated by commas. Do not use adjectives, periods, or line breaks."
